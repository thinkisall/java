ch.클래스 생성자 파트

보통 객체를 만들때, 나눠야하는 부분들이 존재한다.

예를들어, 학생의 공부데이터를 가져와서 점수를 계산한다.라는 내용이 있을 경우

일단 학생에 대한 이름과 학번이 존재할 것이고, 

공부데이터에는 과목과 성적이 있을 것이다.

학생을 class로 만들게되면 결국 속성인 멤버변수로 공부데이터가 들어가게되는데

이 멤버변수부분을 과목 클래스로 별도로 분리해서 관리하는 것이 좋다.

분리하여 사용할 경우 참조의 형식으로 변수들을 받아와야하기때문에

반드시 new 키워드를 통해서 가져와야한다.

아래는 그에 대한 예시 코드이다.